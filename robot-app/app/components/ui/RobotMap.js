"use client";
import { Box, Typography } from "@mui/material";

export default function RobotMap() {
  return (
    <Box sx={{ p: 2, bgcolor: 'background.paper', borderRadius: 1, height: '100%' }}>
      <Typography variant="h6">Robot Map (placeholder)</Typography>
    </Box>
  );
}
